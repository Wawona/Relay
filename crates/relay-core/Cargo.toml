[package]
name = "relay-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Wawona Relay spec, backend enum, and Mode A engine IR"

[features]
default = []
# Kernel HV trap. Mode B tipa / Sileo only. Never on store IPA.
ios-hv = []

[dependencies]
ed25519-dalek = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
